import React from 'react'


const LanguageDetail = props => {
    console.log(props.language)

    return(
      <div>
          {/* <h1> language Details</h1>
          <h2> language title: {props.language.title}</h2>
          <p> language title: {props.language.description}</p>
          <a> other resources: {props.language.resources} </a>
          <button onClick={show posts container for individual language posts }> See more Information about this language</button> */}
      </div>
    )
}







export default LanguageDetail